generator client {
  provider = "prisma-client"
  output   = "./generated/prisma"
  binaryTargets = ["native", "linux-musl"]
  engineType   = "client"
  moduleFormat  = "cjs"
}

datasource db {
  provider = "postgresql"
}
